

# Change material Config settings to use new format in shader and asset loading area , new format is defined in gpu_material_system.h.

# The ECS Engine needs more upgrades , particularly in querying system , 
The problem is that currently we have no way to get a entity id from the chunkArrayView

